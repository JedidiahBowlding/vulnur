# Backend API base URL used by the dashboard
VITE_API_URL=http://localhost:8000

# Optional: if you later protect your backend, set a bearer token here
# and wire it in frontend requests.
VITE_API_BEARER_TOKEN=
